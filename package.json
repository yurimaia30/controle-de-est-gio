{
  "name": "controle-estagio-radiologia",
  "private": true,
  "version": "2.0.0",
  "scripts": {"test": "node --test tests/domain.test.cjs", "test:rules": "node --test tests/firestore.test.mjs", "serve": "node scripts/serve.cjs", "provision": "node scripts/provision-users.mjs", "deploy": "firebase deploy --only firestore:rules,hosting"},
  "devDependencies": {"firebase-admin": "^13.0.0", "firebase-tools": "^14.0.0", "firebase": "^11.0.0", "@firebase/rules-unit-testing": "^4.0.0"}
}
